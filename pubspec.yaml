name: rust_core
description: Implementation of Rust's core library in Dart. Includes types like Result, Option, Cells, Slices, Array, and extensive methods/extensions.
version: 0.5.0-dev3
repository: https://github.com/mcmah309/rust_core

environment:
  sdk: ^3.3.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  #meta: ^1.11.0 # Comment out when pushing. Only used to help write "const" implementations internally

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
